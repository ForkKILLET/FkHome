[user]
	email = fork_killet@qq.com
	name = ForkΨKILLET
[core]
	editor = $VIM/vim
[alias]
	m	= !vim ~/.gitconfig		; Map
	l 	= log --graph --all
	lo	= log --graph --all --oneline
	s	= status
	d 	= diff
	dc	= diff --cached
	a 	= add
	rc	= rm --cached
	c	= commit
	ca	= commit --amend
	co	= checkout
	b	= branch
	bs 	= switch
	dl 	= clone					; Download
	p 	= push
	b	= branch
	ig	= !vim ./.gitignore && git a .gitignore

